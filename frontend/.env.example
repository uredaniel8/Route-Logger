# ===================================================
# Route Logger - Frontend Environment Configuration
# ===================================================
#
# IMPORTANT: Copy this file to .env and fill in your actual values
#
# Example: cp .env.example .env
#
# After making changes, restart the development server:
#   npm start
#

# -----------------------
# Google Maps API Key
# -----------------------
# Required for Map View and Route Optimization features
# 
# To get an API key:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select an existing one
# 3. Enable these APIs:
#    - Maps JavaScript API
#    - Directions API (for route optimization)
#    - Geocoding API (for address lookup)
# 4. Create credentials (API Key)
# 5. Copy the API key below
#
# NOTE: Without this key, the Map View will show a fallback UI
REACT_APP_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# -----------------------
# Backend API URL
# -----------------------
# URL where the Flask backend API is running
#
# Default: http://localhost:5000/api
# 
# For development with proxy (recommended):
REACT_APP_API_URL=/api
#
# For production or different backend location:
# REACT_APP_API_URL=https://your-api-server.com/api
#
# NOTE: The application will fall back to http://localhost:5000/api if not set

